# Studio Environment Variables

# Ambiente de execução (development, production, test)
NODE_ENV=development

# Permitir gravação de páginas em produção (padrão: false)
# Por segurança, operações de escrita (POST, PUT, DELETE) só são permitidas
# em desenvolvimento. Para permitir em produção, defina como 'true'
ALLOW_PAGE_WRITE=false

# Next.js
# Desabilitar telemetria do Next.js (opcional)
# NEXT_TELEMETRY_DISABLED=1
